@{
    Layout = null;
}
@model LoginVM
<!DOCTYPE html>
<!-- Created By CodingNepal -->
<html lang="en" dir="ltr">
<head>
    <meta charset="utf-8">
    <title>Elegencia</title>
    <link rel="icon" href="~/assets/img/favicon.svg" />
    <link rel="stylesheet" href="~/assets/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />
    <link href="~/assets/css/style.css" />

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
 
    <section>
   
        <div class="bg-img">
          
            <div class="content">
                <div class="d-flex justify-content-center">
                    <a asp-controller="home" asp-action="index"><img src="~/assets/img/logo.png" alt="..."></a>
                </div>
                <header>Login</header>
                <form method="post" enctype="multipart/form-data">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="field space">
                        <input asp-for="UsernameOrEmail" required placeholder="Username or Email">
                    </div>
                    <span asp-validation-for="UsernameOrEmail" class="text-danger"></span>


                    <div class="field space">
                        <input asp-for="Password" type="password" class="pass-key" required placeholder="Password">

                    </div>
                    <span asp-validation-for="Password" class="text-danger"></span>
                    <div class="field space">
                        <div class="form-check">
                           
                            <label asp-for="IsRemembered" class="text-light login">
                                <input asp-for="IsRemembered"  style="width:15px; height:15px"  />
                                Remember Me
                            </label>
                        </div>
                    </div>

                    <div class="pass" style="padding-left:300px">
                        <a asp-controller="account" asp-action="forgotpassword">Forgot Password?</a>
                    </div>
                    <div class="field rounded-pill">
                        <input class="rounded-pill" style="border: none" type="submit" value="LOGIN">
                    </div>
                    <div class="pass d-flex justify-content-center">
                        <p class="text-light login">Don't have an account? <a asp-controller="account" asp-action="register">Register Now</a></p>
                    </div>
                </form>
            </div>
        </div>
    </section>
    <script>
        const pass_field = document.querySelector('.pass-key');
        const showBtn = document.querySelector('.show');
        showBtn.addEventListener('click', function () {
            if (pass_field.type === "password") {
                pass_field.type = "text";
                showBtn.textContent = "HIDE";
                showBtn.style.color = "#3498db";
            } else {
                pass_field.type = "password";
                showBtn.textContent = "SHOW";
                showBtn.style.color = "#222";
            }
        });
    </script>
</body>
</html>